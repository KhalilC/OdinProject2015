<h1>All Tags</h1>
<ul>
<% @tags.each do |tag| %>
  <li style="inline"><%= link_to tag.name, tag_path(tag) %></li>
  <i><%= link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "Really delete the '#{tag.name}' tag"} %></i>
<% end %>
</ul>



